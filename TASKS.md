# RelaxMap — задачі команди

Проєкт: **Природні Мандри**. Фронт `relaxmap-front-final`, бек `relaxmap-back-final`.

Команда — 11 людей. Тімлід забирає Header і Footer. У кожного з десяти розробників дві секції і мінімум один ендпоінт. Імена вписуємо в таблицю на старті.

Браузер ніколи не кличе Render напряму. Запит іде в `app/api` на Next, звідти на бекенд. Маршрути бекенда без префікса `/api`: `/auth/register`, `/locations`, `/categories/regions`.

Моделі, Joi-схеми, роути і проксі вже стоять. Хендлери нових ендпоінтів повертають `501`. Твоя робота — замінити `501` на логіку і зібрати свою секцію. Поля моделей не перейменовуємо.

## Що ментор дивиться на 7–8 день

Зустріч до 10 хвилин. Має бути показано саме це.

**Фронт**

- фавікон і назва застосунку
- підключені шрифти
- глобальні CSS-змінні
- публічні і приватні маршрути
- проксі `app/api` на бекенд
- сторінки `/login` і `/register` на 375, 768 і 1440
- головна, хедер і футер на тих самих трьох ширинах
- валідація реєстрації і входу з помилками біля полів
- помилки цих запитів тостами
- після успішного входу або реєстрації редірект на `/profile`
- після перезавантаження авторизований користувач лишається в системі, хедер показує його стан
- вихід з акаунта працює

**Бек**

- бекенд закритий за ТЗ: auth, users, locations, categories, feedbacks

Каталог, сторінка локації, публічний профіль, форма місця і модалка відгуку на цю зустріч не потрібні. Їхні ендпоінти потрібні, бо бекенд перевіряють цілком.

## Порядок до зустрічі

1. Тімлід доводить каркас: фавікон, шрифт з макета, кольори в `styles/globals.css`, хедер, футер, вихід.
2. Реєстрація і вхід закривають свої сторінки і живий логін.
3. Усі власники ендпоінтів замінюють `501` до того, як сідати за пізні сторінки.
4. Перший екран, популярні локації і відгуки на головній підключають дані і верстають три ширини.
5. Сторінки каталогу, деталей, профілю, додавання і редагування локації, модалку відгуку не починаємо, поки не закритий свій ендпоінт і поки головна з авторизацією не тримають дедлайн.

Точки перелому: гумова верстка від 320, адаптив з **375**, планшет **768**, десктоп **1440**. Mobile first, `min-width`, CSS Modules.

## Хто що веде

| Хто | Секції | Ендпоінт | До дедлайну |
| --- | --- | --- | --- |
| Тімлід | Header, Footer | `POST /auth/logout` вже є, підключити в хедері | фавікон, назва, шрифт, змінні, хедер, футер, вихід, три ширини |
| Реєстрація | RegistrationForm, AuthNav | `POST /auth/register` | сторінка `/register`, валідація, тости, редірект |
| Вхід | LoginPage, LoginForm | `POST /auth/login`, `POST /auth/refresh` | сторінка `/login`, валідація, тости, редірект, сесія після reload |
| Перший екран | HeroBlock, AdvantagesBlock | `GET /categories/regions`, `GET /categories/types` | обидві секції на головній і обидва ендпоінти |
| Популярні локації | PopularLocationsBlock, LocationCard | `GET /locations` | карусель на головній і повний список з фільтрами |
| Каталог | FilterPanel, LocationsGrid | `GET /users/:userId/locations` | ендпоінт. Сторінка `/locations` після дедлайну |
| Деталі локації | LocationInfoBlock, LocationGallery | `GET /locations/:locationId` | ендпоінт. Сторінка після дедлайну |
| Профіль | ProfileInfo, ProfilePlaceholder | `GET /users/:userId` | ендпоінт. Сторінка після дедлайну |
| Форма локації | LocationForm, EditLocationPage | `POST /locations`, `PATCH /locations/:locationId` | обидва ендпоінти. Сторінки після дедлайну |
| Відгуки | ReviewsBlock, ReviewsSection | `GET /feedbacks` | слайдер на головній і ендпоінт. Блок на сторінці локації після |
| Новий відгук | AddReviewModal, AddReviewForm | `POST /feedbacks` | ендпоінт. Модалка після дедлайну |

`LocationDescription` робить людина деталей: це текст тієї самої сторінки, окремий файл `components/LocationDescription`. `AuthPromptModal` після дедлайну забирає людина входу.

Каталог не пише другий `GET /locations`. Загальний список з пошуком, регіоном, типом і пагінацією робить розробник популярних локацій, бо ця вибірка потрібна головній ще до зустрічі. Каталог пізніше лише викликає його зі своїєї панелі. Свій ендпоінт каталогу — місця конкретного користувача, їх малює та сама `LocationsGrid` на профілі.

## Тімлід — Header і Footer

Файли: `components/Header`, `components/Footer`, `components/ConfirmationModal`, `app/layout.tsx`, `app/favicon.ico`, `styles/globals.css`, `proxy.ts`.

До дедлайну:

- Фавікон, назва «Relax Map», шрифт Montserrat і CSS-змінні вже взяті з референсу. Хедер і футер лишається довести до макета на цих змінних.
- Хедер для гостя: лого на `/`, «Головна», «Місця відпочинку», «Вхід», «Реєстрація».
- Хедер для авторизованого: лого, «Місця відпочинку», «Мій профіль» на `/profile`, «Поділитись локацією» на `/locations/add`, кнопка виходу. Ім'я береться з `user.name`.
- Вихід відкриває `ConfirmationModal` («Ви впевнені, що хочете вийти?»). Підтвердження кличе `logout()` з `lib/api/clientApi.ts`, чистить стор і веде на `/login`.
- Футер: лого і короткий опис, іконки соцмереж, посилання «Головна» і «Місця відпочинку», копірайт `© 2025 Природні Мандри. Усі права захищені.`
- Хедер, футер і зібрана головна тримають 375, 768, 1440.
- `app/page.tsx` лише складає чотири секції. Логіку секцій туди не переносимо.

Приватні маршрути вже в `proxy.ts`: `/profile`, `/locations/add`, `/locations/:id/edit`. Гостя з них кидає на `/login`. Авторизованого з `/login` і `/register` кидає на `/profile`.

## Реєстрація

Файли: `components/SignUpForm`, `components/AuthNav`, `app/(auth)/register/page.tsx`. Бек: `src/controllers/authController.js` (`registerUser`), схема вже в `src/validations/authValidation.js`.

Поля: `name` 2–32, `email` до 64 і унікальний, `password` 8–128. Помилки Yup біля полів за макетом. Зайнятий email і інші помилки запиту — тост, поля не чистимо. Успіх: користувач авторизований і потрапляє на `/profile`.

`AuthNav` — перемикач «Реєстрація» / «Вхід» на обох auth-сторінках. Активний пункт підсвічений. Сторінка на трьох ширинах.

## Вхід

Файли: `components/SignInForm`, `app/(auth)/login/page.tsx`. Бек: `loginUser` і `refreshUserSession` у `authController.js`.

Поля email і пароль, Yup, тост на помилку запиту, редірект на `/profile`.

Сесія після reload уже зав'язана на `components/AuthProvider`: він кличе `GET /auth/session`, потім `GET /users/me`. Потрібно перевірити це в браузері: залогінився, оновив головну, хедер лишився авторизованим. Якщо access протух, `POST /auth/refresh` має видати нову сесію.

Після дедлайну: `components/AuthPromptModal` для дії гостя, якій потрібен акаунт. Закривається бекдропом, хрестиком і Escape. Кнопки ведуть на `/login` і `/register`.

## Перший екран

Файли: `components/HeroBlock`, `components/AdvantagesBlock`. Бек: `src/controllers/categoryController.js`, модель `src/models/category.js`.

`GET /categories/regions` і `GET /categories/types` повертають масив `{ _id, name, kind }`. `kind` — `region` або `type`.

Hero: заголовок і підзаголовок з ТЗ, пошук веде на `/locations?search=...`. Advantages: три картки «Реальні відгуки», «Зручні фільтри», «Спільнота мандрівників». Тексти в коді чорнові, заміни на макет. Обидві секції на трьох ширинах. Ендпоінти теж до зустрічі: їх чекають фільтри і форма локації.

## Популярні локації

Файли: `components/PopularLocationsBlock`, `components/LocationCard`. Бек: `getLocations` у `src/controllers/locationController.js`.

`GET /locations` віддає

```json
{ "data": [], "page": 1, "limit": 10, "total": 0, "totalPages": 0 }
```

Query: `page`, `limit` (1–100), `region` (id), `type` (id), `search` по назві, `sort` = `rating` (за замовчуванням) або `new`. Картка в `data` має `name`, `type`, `region` (об'єкти категорій), `description`, `images`, `owner` (`_id`, `name`, `avatar`), `rating`, `reviewsCount`.

На головній карусель 4–6 карток: `limit=6&sort=rating`. Стрілки зсувають слайд на один. Картка: фото, тип, назва, «Переглянути локацію» веде на `/locations/[id]`. Іконка редагування лише на картках власного профілю і веде на `/locations/[id]/edit`. Це можна доверстати разом із профілем, після дедлайну. На зустріч потрібні карусель на трьох ширинах і робочий ендпоінт, включно з фільтрами, навіть якщо панель фільтрів ще не на екрані.

## Каталог

Файли: `components/FilterPanel`, `components/LocationsGrid`, `app/locations/page.tsx`. Бек: `getUserLocations` у `src/controllers/userController.js`.

До дедлайну закрий `GET /users/:userId/locations?page=&limit=` у тій самій формі, що й загальний список.

Після дедлайну сторінка `/locations`: пошук, дропдаун регіону, дропдаун типу, сітка, пагінація. Регіони і типи беруться з ендпоінтів першого екрана. Список місць — з `GET /locations`. Запит іде при зміні фільтра і сторінки, на час запиту лоадер. Крайні стрілки пагінації вимкнені на першій і останній сторінці.

## Деталі локації

Файли: `components/LocationInfoBlock`, `components/LocationGallery`, `components/LocationDescription`, `app/locations/[locationId]/page.tsx`. Бек: `getLocationById`.

`GET /locations/:locationId` повертає одну локацію з автором, регіоном і типом. Немає id — 404.

До дедлайну ендпоінт. Після — сторінка: рейтинг, назва, регіон, тип, автор-посилання на `/profile/[userId]`, велике фото, текст опису. `ReviewsSection` на цю сторінку ставить розробник відгуків.

## Профіль

Файли: `components/ProfileInfo`, `components/ProfilePlaceholder`, `app/profile/[userId]/page.tsx`. Бек: `getUserById`.

`GET /users/:userId` віддає публічні `_id`, `name`, `avatar` без пароля і без email.

До дедлайну ендпоінт. Після — сторінка: аватар, ім'я, кількість місць. Список місць рендерить `LocationsGrid` каталогу з `GET /users/:userId/locations`. Якщо місць немає: гість бачить текст з макета і «Назад до локацій» на `/`; власник бачить текст і «Поділитись локацією» на `/locations/add`.

`/profile` лишається приватною сторінкою поточного користувача, на неї веде логін. Коли публічний профіль готовий, «Мій профіль» у хедері можна перевести на `/profile/[свій id]`.

## Форма локації

Файли: `components/LocationForm`, `app/locations/add/page.tsx`, `app/locations/[locationId]/edit/page.tsx`. Бек: `createLocation`, `updateLocation`. Завантаження: `uploadLocationImages` у `src/middleware/multer.js`, далі `saveFileToCloudinary`.

`POST /locations` і `PATCH /locations/:locationId` приватні, тіло `multipart/form-data`: `name` 3–96, `type` і `region` як id категорії, `description` 20–6000, файл `images` jpg/png до 1 МБ. На створення фото обов'язкове. PATCH дозволений лише автору, інакше 403.

До дедлайну обидва ендпоінти. Після — сторінки «Додавання нового місця» і «Редагування місця». Дропдауни з категорій. Прев'ю фото замість плейсхолдера. Formik + Yup. Невалідне — текст біля поля. Запит — лоадер. Успіх створення веде на `/locations/[id]`. Помилка — тост, значення в полях лишаються. «Відмінити» повертає форму до стану на момент відкриття. Редагування спочатку вантажить `GET /locations/:id` і заповнює поля.

## Відгуки

Файли: `components/ReviewsBlock`, `components/ReviewsSection`. Бек: `getFeedbacks`.

`GET /feedbacks` повертає лише `status: "approved"`, форма як у списку локацій.

- без `locationId` — стрічка для головної
- з `locationId` — відгуки цього місця, є `page` і `limit`

Картка: рейтинг, текст, ім'я, тип локації. На головній слайдер зі стрілками, три ширини, дані з бекенда. Це входить у дедлайн, бо головна без цього блоку не зібрана.

Після дедлайну `ReviewsSection` на сторінці локації: заголовок «Відгуки», той самий список і кнопка, що відкриває модалку нового відгуку.

## Новий відгук

Файли: `components/AddReviewModal`, `components/AddReviewForm`. Слот паралельного маршруту вже є: `app/@modal`. Бек: `createFeedback`, модель `src/models/feedback.js`.

`POST /feedbacks` приватний. Тіло: `locationId`, `userName` 2–32, `rate` 1–5, `description` 1–200. Новий відгук зберігається зі `status: "pending"`. У публічний список він не потрапляє, тож після успіху список на сторінці не оновлюємо. Користувач бачить тост, що відгук відправлено на модерацію.

До дедлайну ендпоінт. Після — модалка на паралельному маршруті: 5 зірок, textarea, «Надіслати», «Відмінити». Закриття бекдропом, хрестиком і Escape. Невалідне — помилка біля поля. Помилка запиту — тост, модалка і введені дані лишаються.

## Контракт, який вже зафіксований

Користувач у відповідях: `_id`, `name`, `email` лише для `/users/me`, `avatar`. Пароль назовні не виходить.

Логін після успіху ставить httpOnly-куки `sessionId`, `accessToken`, `refreshToken`. Фронт їх не читає.

Сід для локальної бази: у `relaxmap-back-final` команда `npm run seed`. Вона кладе регіони, типи, демо-користувача `demo@relaxmap.local` / `DemoPass1` і чотири локації з уже схваленими відгуками. Поки ендпоінти `501`, сід лише готує дані.

## Після проміжного дедлайну

- сторінка каталогу
- сторінка локації і опис
- публічний профіль і порожній стан
- створення і редагування локації
- модалка відгуку і `AuthPromptModal`
- `error.tsx` / `not-found.tsx` під макет
- мапа локації і редагування профілю, якщо основні задачі закриті

Їх беруть ті самі власники секцій. Нових людей на це не додаємо.

## Як здавати

Гілка від `dev`, пул-реквест у `dev`. У PR одне речення, який ендпоінт або яка секція. Чужі файли секцій не чіпаємо. Спільні файли каркаса (`layout.tsx`, `page.tsx`, моделі) змінює тімлід, або автор зміни пише йому до коміту.
